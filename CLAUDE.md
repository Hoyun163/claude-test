# CLAUDE.md
本文件用于指导 Claude Code（claude.ai/code）处理该代码仓库内的代码。

## 项目概述
项目初始规则要求项

## 项目结构

- `test.py` — 入门测试脚本
- `.claude/settings.local.json` — Claude Code 本地权限配置

## 环境配置

### Python 环境
- 进入 conda 环境：`conda activate testall` 或 `activate testall`
- 安装库优先使用 conda：`conda install [库名]`
- conda 安装失败则使用 pip：`pip install [库名]`

## 常用命令
```powershell

# 运行日志解析脚本（需先激活 conda 环境）
conda activate testall
```
